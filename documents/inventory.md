# plug-lens 嵌入式Linux视觉AI采集终端 V4.0
## 开源发布前 **全维度最终检查清单**
### 总说明
1. **适用范围**：项目正式开源（GitHub/Gitee）发布、版本打标、代码合并主干前全量核验
2. **依据文件**
   - 《嵌入式Linux视觉AI采集终端 V4.0 全栈代码编写规范》
   - 《plug-lens V4.0 架构体系专用AI提示词》
   - 《plug-lens 专业代码注释编写者 AI提示词》
   - 《plug-lens 开源发布标准注释与优化规范》
   - 《plug-lens 开源社区文档编辑者 AI提示词》
   - 《plug-lens 专业代码审查者 AI提示词》
3. **问题分级（执行强制度）**
   - 🔴 **阻断级**：致命问题，**必须修复**，不允许发布/合并
   - 🟠 **严重级**：功能/安全隐患，**必须修复**
   - 🟡 **警告级**：规范/可读性问题，**建议修复**
   - 🟢 **优化级**：体验/细节优化，**可选修复**
4. **使用方式**：逐项勾选，全部「阻断/严重级」完成后方可执行开源发布；保留核验人、核验日期记录。

---

## 一、工程目录 & 编译体系检查（🔴 阻断级为主）
> 核验目录结构、编译脚本、链接脚本、产物隔离、驱动独立性，匹配物理分层规范

| 级别 | 检查项 | 核验内容 & 规范要求 | 完成☑ |
|------|--------|---------------------|--------|
| 🔴 | 整体目录结构 | 严格遵循约定目录树，无文件乱堆放；`common/`/`src/`/`plugins/`/`drv/`/`documents/`/`output/` 目录完整 | |
| 🔴 | 编译体系完整性 | 顶层`Makefile` + `Makefile.build` 存在；支持C/C++混合编译，编译规则统一 | |
| 🔴 | 自定义链接脚本 | `custom_sections.lds` 完整存在，用于`initcall`自定义段，编译无段地址异常 | |
| 🔴 | 驱动编译隔离 | `drv/led_drv` 完全独立编译，**未纳入应用层Makefile编译链**，仅对接内核源码编译 | |
| 🔴 | 编译产物隔离 | `output/` 仅存放编译产物、缓存文件，已配置进`.gitignore`，不提交代码仓库 | |
| 🔴 | 附属目录规范 | `documents/` 仅存放设计文档、参考资料，不存放业务代码；不参与编译 | |
| 🟡 | 插件目录划分 | `plugins` 下 `app_plugins`/`base_plugins`/`service_plugins` 分类准确，职责不交叉 | |
| 🟡 | 头文件/源文件分区 | 所有模块统一 `inc`(头文件) + `src`(源文件) 目录结构，格式统一 | |

---

## 二、架构合规性专项检查（🔴 阻断级，架构红线，最高优先级）
> 核验四层逻辑分层、单向依赖、模块通信规则、核心组件职责边界，严禁架构越界

### 2.1 分层与依赖规则
| 级别 | 检查项 | 核验内容 & 规范要求 | 完成☑ |
|------|--------|---------------------|--------|
| 🔴 | 单向依赖原则 | 四层逻辑架构**自上而下单向依赖**：应用编排层 → 业务服务层 → 中间枢纽层 → 系统基建层；下层无任何反向依赖上层代码 | |
| 🔴 | 跨层调用禁止 | 应用层不直接调用基建层私有接口；服务层不直接调用其他服务内部函数；无跨层直连代码 | |
| 🔴 | 硬件访问规则 | 所有硬件操作通过 `src/base` 抽象基类 + 插件实现，**应用代码不直接操作内核驱动** | |

### 2.2 双总线 & 数据链路职责边界（项目核心红线）
| 级别 | 检查项 | 核验内容 & 规范要求 | 完成☑ |
|------|--------|---------------------|--------|
| 🔴 | event_bus 事件总线 | 仅传输控制命令、状态、异常、启停指令；纯控制流，不传输大数据帧 | |
| 🔴 | data_bus 数据总线 | 仅做**帧句柄纯转发/路由**；不修改数据、不做格式转换、不分配/释放内存 | |
| 🔴 | frame_link 应用数据链路 | 仅负责**帧生命周期管理(内存池/引用计数/帧回收)** + 预留协议封装；**不承载YUYV/RGB转换、JPEG/H264编解码**等运算逻辑 | |
| 🔴 | 编解码/格式转换归属 | YUYV↔RGB、JPEG压缩收敛至 `common/img_proc`；H264编码收敛至推流服务内部；未侵入总线/链路层 | |

### 2.3 插件 & 编译期自动初始化
| 级别 | 检查项 | 核验内容 & 规范要求 | 完成☑ |
|------|--------|---------------------|--------|
| 🔴 | 自动初始化宏使用 | 所有模块初始化函数统一使用 `INIT_*_EXPORT` 系列宏，按优先级注册；未手动修改`main`初始化流程 | |
| 🔴 | 初始化优先级匹配 | 板级/软件/设备/组件/应用等初始化宏优先级使用正确，启动顺序无错乱 | |
| 🟡 | 插件职责划分 | `app_plugins`(流程编排)、`base_plugins`(硬件/算法实现)、`service_plugins`(业务服务) 职责清晰不混用 | |

### 2.4 预留组件规范
| 级别 | 检查项 | 核验内容 & 规范要求 | 完成☑ |
|------|--------|---------------------|--------|
| 🟢 | fsm 状态机 | `src/fsm` 为预留组件，当前业务代码未强行依赖、未误用 | |
| 🟢 | audio_link | 音频链路为预留模块，现有视频业务未异常调用 | |

---

## 三、代码编译 & 基础语法检查（🔴 阻断级）
> 核验编译结果、语法、头文件规范、C/C++混合编译、类型安全

| 级别 | 检查项 | 核验内容 & 规范要求 | 完成☑ |
|------|--------|---------------------|--------|
| 🔴 | 整体编译结果 | 全工程编译 **0错误、0警告**（警告视同错误） | |
| 🔴 | 头文件保护 | 所有`.h`/`.hpp` 头文件均添加标准头文件保护宏，无重复包含问题 | |
| 🔴 | C++兼容防护 | **所有对外公开头文件** 强制添加 `extern "C"` 代码块，C/C++混合调用兼容正常 | |
| 🔴 | 标准固定类型 | 全程使用 `stdint.h` 固定宽度类型 `uint8_t/int32_t` 等；禁止直接使用 `int/short/long` 原生类型 | |
| 🔴 | 隐式函数声明 | 无隐式函数声明、类型不匹配、函数原型缺失问题 | |
| 🟡 | 废弃代码清理 | 已删除所有注释掉的代码、临时调试代码、冗余死代码 | |

---

## 四、函数设计 & 代码质量检查（🟠 严重级 / 🟡 警告级）
> 核验单一职责、函数结构、命名、格式、const/static、DRY原则等Clean Code规范

### 4.1 单一职责 & 函数结构（核心要求）
| 级别 | 检查项 | 核验内容 & 规范要求 | 完成☑ |
|------|--------|---------------------|--------|
| 🟠 | 函数单一职责 | 每个函数**只做一件事**；无「验证+发送」「初始化+启动」等混合职责函数 | |
| 🟠 | 函数长度 | 单个函数代码行 ≤ 80行；超长函数已拆分为子函数 | |
| 🟠 | 嵌套深度 | 代码嵌套层级 ≤ 4层；大量嵌套已使用**提前返回**优化 | |
| 🟠 | 分支语句规范 | 所有`if/for/while/do-while` 即使单行也必须加大括号；`switch` 均包含`default`分支 | |
| 🟡 | 参数数量 | 函数形参 ≤ 5个；超量参数已封装为配置结构体 | |
| 🟡 | 命令-查询分离 | 函数要么执行操作，要么查询信息，不同时兼具两种行为 | |

### 4.2 命名 & 格式规范
| 级别 | 检查项 | 核验内容 & 规范要求 | 完成☑ |
|------|--------|---------------------|--------|
| 🟡 | 统一命名规则 | 宏(全大写蛇形)、类型(PascalCase+_t)、函数(模块_动作)、局部变量(小写蛇形)、静态变量(s_前缀)、全局变量(g_前缀) 完全合规 | |
| 🟡 | 布尔变量命名 | 布尔类型统一使用 `is_/has_/can_/should_` 前缀 | |
| 🟡 | 代码行宽 | 单行代码 ≤ 80列，超长代码在逻辑断点换行 | |
| 🟡 | 缩进与符号 | 统一缩进格式；运算符、逗号前后空格规范，风格全局统一 | |

### 4.3 const / static 与 通用规范
| 级别 | 检查项 | 核验内容 & 规范要求 | 完成☑ |
|------|--------|---------------------|--------|
| 🟠 | const 正确性 | 所有只读指针、只读变量均添加`const`；无滥用/缺失const场景 | |
| 🟠 | static 限制 | 所有非对外函数、文件级私有变量均声明为`static`，作用域隔离 | |
| 🟡 | 魔法数字 | 除0/1外，所有字面常量均定义为宏；代码中无裸数字 | |
| 🟡 | DRY原则 | 重复逻辑已抽取为公共函数；无复制粘贴冗余代码 | |
| 🟢 | 表驱动优化 | 复杂if-else/switch 优先使用表驱动实现（可选优化） | |

### 4.4 C-OOP 面向对象规范（项目专属）
| 级别 | 检查项 | 核验内容 & 规范要求 | 完成☑ |
|------|--------|---------------------|--------|
| 🟠 | 不透明指针 | 对外头文件使用 `typedef struct X X_t;` 不透明指针；外部禁止直接访问结构体成员 | |
| 🟠 | 继承规则 | 派生类结构体将基类作为**首个成员**；类型转换优先使用`container_of`，禁止强制类型转换 | |
| 🟡 | 虚函数表 | 多态统一使用函数指针表实现，符合开闭原则 | |
| 🟡 | 函数排序 | 头文件/源文件函数按生命周期排序：init → 配置 → start → 运行 → pause → resume → stop → deinit | |

---

## 五、内存安全 & 帧管理专项检查（🟠 严重级，嵌入式核心安全项）
> 核验堆内存、栈、缓冲区、frame_link帧生命周期、内存池规则

| 级别 | 检查项 | 核验内容 & 规范要求 | 完成☑ |
|------|--------|---------------------|--------|
| 🟠 | 堆内存配对 | `malloc`/`free` 严格配对；所有内存分配均校验NULL返回值 | |
| 🟠 | 错误路径资源清理 | 函数异常/返回失败时，已分配内存、句柄、锁等资源全部清理；使用`goto`统一清理逻辑 | |
| 🟠 | 野指针防护 | 内存`free`后指针主动置`NULL`；无释放后使用、重复释放 | |
| 🟠 | 栈溢出防护 | 禁止变长数组(VLA)、禁止递归；大型局部变量改用堆分配 | |
| 🟠 | 缓冲区安全 | 禁用`strcpy/sprintf`等危险函数；统一使用`strncpy/snprintf`等带长度校验接口 | |
| 🔴 | FrameLink 帧管理 | 所有视频帧统一由`frame_link`分配/回收；引用计数增减逻辑正确，无漏加、漏减 | |
| 🔴 | 内存池规则 | 大块视频帧使用静态内存池；运行时无频繁动态内存申请释放 | |

---

## 六、线程安全 & 硬件交互检查（🟠 严重级）
> 核验多线程、中断、互斥保护、硬件阻塞行为、DMA/中断规范

| 级别 | 检查项 | 核验内容 & 规范要求 | 完成☑ |
|------|--------|---------------------|--------|
| 🟠 | 共享资源保护 | 线程间共享变量使用互斥锁/信号量；读多写少场景合理使用读写锁 | |
| 🟠 | 锁使用规范 | 互斥锁加锁/解锁严格配对；不持有锁执行耗时操作；加锁顺序统一，无死锁风险 | |
| 🟠 | 中断(ISR)规范 | 中断服务函数极简，仅标记标志/释放信号量；ISR内**不分配内存、不调用复杂函数、不阻塞** | |
| 🟠 | 驱动非阻塞 | 所有硬件API为**非阻塞异步模式**；阻塞逻辑收敛至驱动内部工作线程 | |
| 🟡 | 线程通信 | 线程间优先使用消息队列通信，减少共享变量依赖 | |
| 🟡 | 回调函数规范 | 回调函数简短非阻塞；耗时逻辑已投递至业务队列，未在回调内执行 | |

---

## 七、错误处理 & 防御性编程检查（🟠 严重级）
| 级别 | 检查项 | 核验内容 & 规范要求 | 完成☑ |
|------|--------|---------------------|--------|
| 🟠 | 统一错误码 | 全程使用项目自定义错误码枚举；代码中无裸整数错误返回值 | |
| 🟠 | 返回值校验 | 所有系统调用、库函数、自定义函数返回值均被检查；无静默忽略错误 | |
| 🟠 | 错误向上传播 | 错误沿调用链逐层上报，不中途吞掉异常 | |
| 🟡 | 断言区分 | 断言用于校验内部契约；运行时判断用于校验外部输入，二者使用场景正确 | |

---

## 八、代码注释专项检查（🟡 警告级，对标注释编写规范）
| 级别 | 检查项 | 核验内容 & 规范要求 | 完成☑ |
|------|--------|---------------------|--------|
| 🟡 | 文件头部注释 | 所有`.c`/`.h` 文件均包含标准头部注释：作者、版本、日期、Github、MIT协议、功能说明 | |
| 🟡 | 对外接口注释 | 头文件公共函数/结构体/枚举/宏 使用 **中英文双语注释**；字段完整（@brief/@param/@return/@pre/@note/@warning等） | |
| 🟡 | 内部代码注释 | 源文件私有实现使用**纯中文注释**；重点解释「设计思路/原因」，不重复描述代码行为 | |
| 🟡 | 核心逻辑注释 | 多线程、内存管理、算法、优化点、历史BUG修复点均添加对应注释 | |
| 🟢 | 注释时效性 | 代码修改后同步更新对应注释，无注释与代码不一致场景 | |

---

## 九、开源合规 & 信息脱敏检查（🔴 阻断级，开源必备）
| 级别 | 检查项 | 核验内容 & 规范要求 | 完成☑ |
|------|--------|---------------------|--------|
| 🔴 | 协议文件 | 根目录存在 `LICENSE` 文件，为标准 MIT 协议，版权信息正确 | |
| 🔴 | 公开文档/代码脱敏 | 代码、公开文档中**已移除真实IP、绝对路径、个人隐私、硬编码密码**；IP统一替换为`192.168.1.100`，路径替换为`/root/plug-lens/` | |
| 🔴 | .gitignore 配置 | 完整过滤编译产物、临时文件、IDE配置、日志文件等无用文件 | |
| 🟡 | 版权声明 | 所有代码文件头部版权声明统一，与MIT协议一致 | |
| 🟡 | 第三方组件合规 | 所用 libjpeg-turbo / Live555 / MNN 等第三方库，协议兼容MIT，无侵权代码 | |

---

## 十、开源文档体系检查（🟡 警告级，对标文档编辑规范）
> 适配 GitHub/Gitee 双语文档规则、由浅入深阅读逻辑

| 级别 | 检查项 | 核验内容 & 规范要求 | 完成☑ |
|------|--------|---------------------|--------|
| 🔴 | 双语文档完整 | 主文档存在 `README.md`(英文) + `README_zh-CN.md`(中文)；内容完全同步 | |
| 🟡 | 文档结构 | 文档遵循「快速上手 → 功能介绍 → 架构解析 → 开发指南 → FAQ → 贡献指南」由浅入深逻辑 | |
| 🟡 | 命令/示例可用 | 所有Shell命令、代码示例可直接复现，无错误、无绝对路径 | |
| 🟡 | 内部链接有效 | 文档内相对路径链接正常，无死链接 | |
| 🟡 | 附属文档齐全 | `docs/` 下 快速上手、架构文档、开发指南、部署手册、FAQ、CONTRIBUTING.md 贡献指南完整 | |
| 🟢 | 格式规范 | 全部使用 GitHub Flavored Markdown 语法，排版整洁 | |

---

## 十一、功能 & 稳定性最终实测（🔴 阻断级）
> 发布前整机运行、异常、压力测试核验

| 级别 | 检查项 | 核验内容 & 规范要求 | 完成☑ |
|------|--------|---------------------|--------|
| 🔴 | 基础功能全通 | 采集、人脸检测、RTSP推流、SD抓拍、LED指示 核心功能运行正常 | |
| 🔴 | 异常容错 | 摄像头断开、网络断连、SD卡异常后，系统自动重连/容错，不崩溃 | |
| 🔴 | 守护进程 | 守护进程保活功能正常，进程异常退出后可自动拉起 | |
| 🟠 | 长期稳定性 | 连续72小时压力测试，无内存泄漏、无卡死、无帧率骤降 | |
| 🟡 | 性能指标达标 | 帧率、端到端延迟、CPU/内存占用 符合设计指标 | |
| 🟡 | 开机自启 | 开机自启脚本/配置运行正常（按需） | |

---

## 十二、Git & 版本发布收尾检查（🔴 阻断级）
| 级别 | 检查项 | 核验内容 & 规范要求 | 完成☑ |
|------|--------|---------------------|--------|
| 🔴 | 代码提交 | 所有待发布代码已提交至本地仓库，无未跟踪、未提交的临时文件 | |
| 🔴 | 版本标签 | 已创建正式版本标签 `git tag v1.0.0`，标签命名与项目版本一致 | |
| 🔴 | 远程推送 | 代码+标签已正常推送到 GitHub、Gitee 双平台 | |
| 🟡 | 提交记录 | 提交日志清晰规范，无大量临时「test/debug」类提交 | |

---

## 十三、整体复核（最终签字确认）
1. 所有 **🔴 阻断级、🟠 严重级** 问题已100%修复完毕
2. 项目完全符合：V4.0架构规范 + 代码编写规范 + 注释规范 + 开源文档规范
3. 编译、运行、功能、稳定性、开源合规全部核验通过

**核验人**：__________
**核验日期**：__________
**最终结论**：□ 允许正式开源发布  □ 存在问题，暂缓发布