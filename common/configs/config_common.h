/* SPDX-License-Identifier: MIT
 * Copyright (c) 2026 LuoZhihong (相醉为友)
 * All rights reserved.
 */

/**
 * @file    config_common.h
 * @brief   Common global configuration macros for plug-lens project
 *          plug-lens 项目全局通用配置宏定义
 * @details 核心配置说明：
 *          1. 统一定义嵌入式Linux终端日志、SD卡存储、工作目录基础路径
 *          2. 集成网络状态检测、NTP时间同步功能总开关与参数配置
 *          3. 所有参数支持编译期裁剪，适配工业级部署场景
 *
 * @author  LuoZhihong
 * @github  https://github.com/zhihong1469/plug-lens
 * @date    2026-05-29
 * @version v1.0.0
 * @license MIT License
 *
 * @note    全局配置注意事项：
 *          1. 路径配置与硬件挂载目录强绑定，修改前需确认系统目录结构
 *          2. 功能开关宏(USE_xxx)为1=启用，0=禁用，用于编译期裁剪模块
 *          3. 网络参数适配国内工业环境，采用公共DNS/阿里云NTP服务器
 */
#ifndef __CONFIG_COMMON_H__
#define __CONFIG_COMMON_H__

#include <stdbool.h>

#ifdef __cplusplus
extern "C" {
#endif

/** SD卡功能启用开关 | 1=启用SD卡存储，0=禁用SD卡功能 */
#ifndef USE_SD
#define USE_SD 1
#endif

// ==========================================================================
// 日志配置（工业级可配置）
// ==========================================================================
/** 工业级应用日志文件存储路径 | 日志持久化存储绝对路径 */
#define LOG_FILE_PATH                  "/mnt/sdcard/log/app.log"
/** 日志文件滚动阈值 | 单文件最大10MB，超出自动切割 */
#define LOG_MAX_FILE_SIZE (10 * 1024 * 1024)

// ==========================================================================
// SD卡存储配置（工业级可配置）
// ==========================================================================
/** SD卡挂载根目录 | 系统SD卡标准挂载路径 */
#define CONFIG_SD_STORAGE_ROOT_PATH           "/mnt/sdcard"
/** 视觉采集数据存储目录 | 人脸/图像采集文件保存路径 */
#define CONFIG_SD_STORAGE_DIR                 "/mnt/sdcard/face_capture"

// ==========================================================================
// 系统工作目录配置（固定不可修改）
// ==========================================================================
/** 程序守护进程工作目录 | 与板端目录结构严格绑定，禁止修改 */
#define DAEMON_WORK_DIR    "/root/run_on_board"

// ==========================================================================
// 核心功能总开关（编译期裁剪）
// ==========================================================================
/** 网络状态检测功能开关 | 1=启用，0=禁用 */
#define USE_NET_CHECK      1
/** NTP网络时间同步功能开关 | 1=启用，0=禁用 */
#define USE_NET_TIME_SYNC  1

// ==========================================================================
// 网络状态检测参数配置
// ==========================================================================
/** 网络检测超时时间 | 单位：秒，超时判定网络断开 */
#define NET_CHECK_TIMEOUT  2
/** DNS解析服务器地址 | 国内公共114DNS，保障网络连通性检测 */
#define NET_DNS_SERVER     "114.114.114.114"
/** DNS服务标准端口 | 固定53端口，禁止修改 */
#define NET_DNS_PORT       53
/** 网口设备名称 | 嵌入式Linux板端以太网设备名 */
#define NET_DEV_NAME       "eth0"

// ==========================================================================
// NTP网络时间同步参数配置
// ==========================================================================
/** NTP服务器地址 | 阿里云公共NTP服务器，适配国内工业网络 */
#define NTP_SERVER_ADDR    "time1.aliyun.com"

#ifdef __cplusplus
}
#endif

#endif /* __CONFIG_COMMON_H__ */