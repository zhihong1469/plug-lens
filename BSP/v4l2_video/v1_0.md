# v4l2 多线程死锁问题 V1.0
## 【启动阶段】main
v4l2_app_init——init
v4l2_app_start——【Capture 线程】
main_while——Main线程（消费者）

## v4l2_app
Capture 线程:
【Capture 线程】:_capture_thread_func
    ↓
_loop:
    ↓
1. _pool_get_free_item()  [加 g_pool_mutex -> 拿空Item -> 解锁]
    ↓
2. v4l2_video_get_frame(&item->frame)  【这里是关键！】
    ↓
    【进入 v4l2_video.c】
        ↓
    2.1 pthread_mutex_lock(&g_mutex)  <--- 🔴 在这里拿到了锁！
        ↓
    2.2 检查 g_is_streaming (Yes)
        ↓ 
    > 能否加个信号量呢？
    2.3 poll(..., 3000)  <--- 注意：它是拿着锁在 poll！
        ↓
        (摄像头数据来了)
        ↓
    2.4 ioctl(VIDIOC_DQBUF)  <--- 从驱动拿出一帧
        ↓
    2.5 填充 frame->data, frame->index
        ↓
    2.6 【注意】没有 unlock！直接 return！
    > 持锁退出!
        ↓
    【退出 v4l2_video.c】
    ↓
3. Capture 线程现在持有 🔑 g_mutex！
    ↓
4. Queue_Put(..., item)  [加 Queue 锁 -> 塞进去 -> 解锁]
    ↓
5. 【注意】它还没有释放 g_mutex！
    ↓
6. 跳回 _loop 进行下一轮


## 总结：有点乱，感觉bsp层和app层有点没分干净……